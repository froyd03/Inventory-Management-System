services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: frdbntao/frontend:1.0
    ports:
      - 5173:5173
    
    environment:
      - CHOKIDAR_USEPOLLING=true
    stdin_open: true
    tty: true
    depends_on:
      - backend  

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: frdbntao/backend:1.0
    ports:
      - 5000:5000
    command: npm run dev
